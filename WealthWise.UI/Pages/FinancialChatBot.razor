@page "/financial-chat"
@using WealthWise.UI.Services
@using System.Net.Http.Json
@using System.Text.Json
@using Microsoft.AspNetCore.Components.Web

@* @inject AiChatService ChatService // Need to fix the issue *@
 
<div class="card chat-fullwidth">
    <div class="header-section">
        <h1 class="main-title">🤖 AI Financial Assistant</h1>
    </div>

    <div class="chat-inner">
        <div class="chat-container">
            @foreach (var msg in messages)
            {
                <div class="chat-message @(msg.Role)">
                    <div class="bubble">
                        <p>@msg.Content</p>
                    </div>
                </div>
            }

            @if (isTyping)
            {
                <div class="chat-message assistant">
                    <div class="bubble typing">...</div>
                </div>
            }
        </div>

        <div class="chat-input">
            <input @bind="userInput" placeholder="Type your message..." @onkeydown="@OnKeyDown" />
            <button @onclick="SendMessage">Send</button>
        </div>
    </div>

</div>

@code {
    private List<ChatMessage> messages = new()
    {
        new ChatMessage("assistant", "Hello! I'm your Financial Assistent. How can I help you today?")
    };

    private string userInput = "";
    private bool isTyping = false;
    private readonly HttpClient _httpClient = new();

    protected override void OnInitialized()
    {
        _httpClient.BaseAddress = new Uri("http://localhost:8000"); // API URL
    }

    private async Task SendMessage()
    {
        if (string.IsNullOrWhiteSpace(userInput)) return;

        var userMsg = new ChatMessage("user", userInput);
        messages.Add(userMsg);

        var input = userInput;
        userInput = "";
        isTyping = true;

        StateHasChanged();

        try
        {
            // Use escaped query param and avoid adding extra quotes around the message
            var url = $"/financial/chat?message={Uri.EscapeDataString(input)}";
            var apiResponse = await _httpClient.PostAsync(url, null);

            if (apiResponse.IsSuccessStatusCode)
            {
                string reply = null;

                // Try to read the JSON string response and unescape it so "\n" becomes real newlines
                try
                {
                    reply = await apiResponse.Content.ReadFromJsonAsync<string>();
                }
                catch
                {
                    var raw = await apiResponse.Content.ReadAsStringAsync();
                    try
                    {
                        reply = JsonSerializer.Deserialize<string>(raw);
                    }
                    catch
                    {
                        // fallback to raw content if deserialization fails
                        reply = raw;
                    }
                }

                if (string.IsNullOrEmpty(reply))
                {
                    messages.Add(new ChatMessage("assistant", "⚠️ Error: Empty response from server."));
                }
                else
                {
                    messages.Add(new ChatMessage("assistant", reply));
                }
            }
            else
            {
                messages.Add(new ChatMessage("assistant", "⚠️ Error: Unable to get response from the server."));
            }
        }
        catch (Exception ex)
        {
            messages.Add(new ChatMessage("assistant", $"⚠️ Error: {ex.Message}"));
        }
        finally
        {
            isTyping = false;
            StateHasChanged();
        }
    }

    private async Task OnKeyDown(KeyboardEventArgs e)
    {
        if (e.Key == "Enter")
        {
            await SendMessage();
        }
    }

    public class ChatMessage
    {
        public string Role { get; set; } // "user" or "assistant"
        public string Content { get; set; }
        public ChatMessage(string role, string content)
        {
            Role = role;
            Content = content;
        }
    }
}
